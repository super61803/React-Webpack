import styles from "../App.module.scss";

const About = () => {
  return (
    <p className={styles.message}>
      React Webpack boilerplate with TypeScript, Sass, and React Router.
    </p>
  );
};

export default About;
